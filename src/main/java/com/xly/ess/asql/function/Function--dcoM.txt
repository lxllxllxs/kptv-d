DELIMITER $$

USE `xly`$$

DROP FUNCTION IF EXISTS `dcoM`$$

CREATE DEFINER=`root`@`%` FUNCTION `dcoM`(ctmId INT) RETURNS DOUBLE
BEGIN
    DECLARE V_MONEY DOUBLE DEFAULT 0;
    SELECT DECODE(money,'xly') INTO V_MONEY FROM xly_customer WHERE CTM_ID=ctmId;
	RETURN V_MONEY;
    END$$

DELIMITER ;