DELIMITER $$

USE `xly`$$

DROP PROCEDURE IF EXISTS `dcoMoney`$$

CREATE DEFINER=`root`@`%` PROCEDURE `dcoMoney`(IN ctmId INT)
BEGIN
	 SELECT DECODE(money,'xly') money FROM xly_customer WHERE CTM_ID=ctmId;
    END$$

DELIMITER ;